{
  "compilerOptions": {
    "target": "ESNext", // 编译输出的 ECMAScript 目标版本
    "module": "ESNext", // 模块系统格式
    "moduleResolution": "Node", // 模块解析策略，适配 Node 环境
    "skipLibCheck": true, // 跳过声明文件（.d.ts）的类型检查，加快编译
    "esModuleInterop": true, // 支持默认导入 CommonJS 模块
    "allowSyntheticDefaultImports": true, // 允许对没有默认导出的模块进行默认导入
    "sourceMap": true, // 生成 sourceMap 文件，方便调试
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"] // 路径别名配置，方便 import 时使用
    },
    "lib": ["ES2015", "DOM"], // 包含的运行时库
    "allowJs": true, // 允许编译 JS 文件，这对纯 JS 项目关键
    "checkJs": false // 不检查 JS 文件的类型错误，符合你项目情况
  },
  "include": ["src/**/*.js", "src/**/*.vue"], // 只包含 JS 和 Vue 文件，因为没有 TS 相关文件了
  "exclude": ["node_modules", "dist"]
}